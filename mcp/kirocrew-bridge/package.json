{
  "name": "kirocrew-bridge",
  "version": "1.0.0",
  "description": "MCP bridge exposing KiroCrew gateway tools to opencode",
  "type": "module",
  "bin": {
    "kirocrew-bridge": "./dist/index.js"
  },
  "scripts": {
    "build": "npx esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --format=cjs --external:electron",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0"
  }
}
